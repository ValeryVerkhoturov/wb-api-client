/*
 * Маркетинг и продвижение
 * Узнать больше о маркетинге и продвижении можно в [справочном центре](https://seller.wildberries.ru/instructions/category/59d92bd3-6ea0-40f2-b762-ca8835d7d42e?goBackOption=prevRoute&categoryId=479385c6-de01-4b4d-ad4e-ed941e65582e)  Методы маркетинга и продвижения позволяют: 1. Получать информацию о кампаниях [продвижения](https://dev.wildberries.ru/openapi/promotion#tag/campaigns) и [медиакампаниях](https://dev.wildberries.ru/openapi/promotion#tag/media) 2. [Создавать](https://dev.wildberries.ru/openapi/promotion#tag/creatingCampaigns) и [управлять](https://dev.wildberries.ru/openapi/promotion#tag/campaignManagement) кампаниями 3. Управлять [финансами](https://dev.wildberries.ru/openapi/promotion#tag/finances) кампаний 4. Выгружать [статистику](https://dev.wildberries.ru/openapi/promotion#tag/statistics) кампаний продвижения и медиакампаний 5. Работать с [календарём акций](https://dev.wildberries.ru/openapi/promotion#tag/promoCalendar) Данные синхронизируются с базой раз в 3 минуты. Статусы кампаний меняются раз в минуту. Ставки кампаний меняются раз в 30 секунд.  Вы можете протестировать методы продвижения в [песочнице](https://dev.wildberries.ru/sandbox). Также в песочнице доступны [специальные методы](https://dev.wildberries.ru/docs/openapi-other/sandbox-environment#tag/promotion) для управления тестовым балансом
 *
 * The version of the OpenAPI document: promotion
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.valeryverkhoturov.wbapi.promotion.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.valeryverkhoturov.wbapi.promotion.model.Stat;
import io.github.valeryverkhoturov.wbapi.promotion.model.StatCampaignNotFound;
import io.github.valeryverkhoturov.wbapi.promotion.model.StatDate;
import io.github.valeryverkhoturov.wbapi.promotion.model.StatInterval;
import io.github.valeryverkhoturov.wbapi.promotion.model.StatIntervalInterval;
import io.github.valeryverkhoturov.wbapi.promotion.model.StatsBlok1;
import java.io.IOException;
import java.time.LocalDate;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import io.github.valeryverkhoturov.wbapi.promotion.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.10.0")
public class PostV1StatsResponse200Inner extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(PostV1StatsResponse200Inner.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PostV1StatsResponse200Inner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PostV1StatsResponse200Inner' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<StatInterval> adapterStatInterval = gson.getDelegateAdapter(this, TypeToken.get(StatInterval.class));
            final TypeAdapter<StatDate> adapterStatDate = gson.getDelegateAdapter(this, TypeToken.get(StatDate.class));
            final TypeAdapter<Stat> adapterStat = gson.getDelegateAdapter(this, TypeToken.get(Stat.class));
            final TypeAdapter<StatCampaignNotFound> adapterStatCampaignNotFound = gson.getDelegateAdapter(this, TypeToken.get(StatCampaignNotFound.class));

            return (TypeAdapter<T>) new TypeAdapter<PostV1StatsResponse200Inner>() {
                @Override
                public void write(JsonWriter out, PostV1StatsResponse200Inner value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `StatInterval`
                    if (value.getActualInstance() instanceof StatInterval) {
                        JsonElement element = adapterStatInterval.toJsonTree((StatInterval)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `StatDate`
                    if (value.getActualInstance() instanceof StatDate) {
                        JsonElement element = adapterStatDate.toJsonTree((StatDate)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Stat`
                    if (value.getActualInstance() instanceof Stat) {
                        JsonElement element = adapterStat.toJsonTree((Stat)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `StatCampaignNotFound`
                    if (value.getActualInstance() instanceof StatCampaignNotFound) {
                        JsonElement element = adapterStatCampaignNotFound.toJsonTree((StatCampaignNotFound)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Stat, StatCampaignNotFound, StatDate, StatInterval");
                }

                @Override
                public PostV1StatsResponse200Inner read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize StatInterval
                    try {
                        // validate the JSON object to see if any exception is thrown
                        StatInterval.validateJsonElement(jsonElement);
                        actualAdapter = adapterStatInterval;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'StatInterval'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for StatInterval failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'StatInterval'", e);
                    }
                    // deserialize StatDate
                    try {
                        // validate the JSON object to see if any exception is thrown
                        StatDate.validateJsonElement(jsonElement);
                        actualAdapter = adapterStatDate;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'StatDate'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for StatDate failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'StatDate'", e);
                    }
                    // deserialize Stat
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Stat.validateJsonElement(jsonElement);
                        actualAdapter = adapterStat;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Stat'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Stat failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Stat'", e);
                    }
                    // deserialize StatCampaignNotFound
                    try {
                        // validate the JSON object to see if any exception is thrown
                        StatCampaignNotFound.validateJsonElement(jsonElement);
                        actualAdapter = adapterStatCampaignNotFound;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'StatCampaignNotFound'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for StatCampaignNotFound failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'StatCampaignNotFound'", e);
                    }

                    if (match == 1) {
                        PostV1StatsResponse200Inner ret = new PostV1StatsResponse200Inner();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for PostV1StatsResponse200Inner: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public PostV1StatsResponse200Inner() {
        super("oneOf", Boolean.FALSE);
    }

    public PostV1StatsResponse200Inner(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("StatInterval", StatInterval.class);
        schemas.put("StatDate", StatDate.class);
        schemas.put("Stat", Stat.class);
        schemas.put("StatCampaignNotFound", StatCampaignNotFound.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return PostV1StatsResponse200Inner.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Stat, StatCampaignNotFound, StatDate, StatInterval
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof StatInterval) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof StatDate) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Stat) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof StatCampaignNotFound) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Stat, StatCampaignNotFound, StatDate, StatInterval");
    }

    /**
     * Get the actual instance, which can be the following:
     * Stat, StatCampaignNotFound, StatDate, StatInterval
     *
     * @return The actual instance (Stat, StatCampaignNotFound, StatDate, StatInterval)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `StatInterval`. If the actual instance is not `StatInterval`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `StatInterval`
     * @throws ClassCastException if the instance is not `StatInterval`
     */
    public StatInterval getStatInterval() throws ClassCastException {
        return (StatInterval)super.getActualInstance();
    }

    /**
     * Get the actual instance of `StatDate`. If the actual instance is not `StatDate`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `StatDate`
     * @throws ClassCastException if the instance is not `StatDate`
     */
    public StatDate getStatDate() throws ClassCastException {
        return (StatDate)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Stat`. If the actual instance is not `Stat`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Stat`
     * @throws ClassCastException if the instance is not `Stat`
     */
    public Stat getStat() throws ClassCastException {
        return (Stat)super.getActualInstance();
    }

    /**
     * Get the actual instance of `StatCampaignNotFound`. If the actual instance is not `StatCampaignNotFound`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `StatCampaignNotFound`
     * @throws ClassCastException if the instance is not `StatCampaignNotFound`
     */
    public StatCampaignNotFound getStatCampaignNotFound() throws ClassCastException {
        return (StatCampaignNotFound)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PostV1StatsResponse200Inner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with StatInterval
        try {
            StatInterval.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for StatInterval failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with StatDate
        try {
            StatDate.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for StatDate failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Stat
        try {
            Stat.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Stat failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with StatCampaignNotFound
        try {
            StatCampaignNotFound.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for StatCampaignNotFound failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for PostV1StatsResponse200Inner with oneOf schemas: Stat, StatCampaignNotFound, StatDate, StatInterval. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of PostV1StatsResponse200Inner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PostV1StatsResponse200Inner
     * @throws IOException if the JSON string is invalid with respect to PostV1StatsResponse200Inner
     */
    public static PostV1StatsResponse200Inner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PostV1StatsResponse200Inner.class);
    }

    /**
     * Convert an instance of PostV1StatsResponse200Inner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

