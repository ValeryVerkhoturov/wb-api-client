// FinancesGetDocsData
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// fileName - Строка - Название документа
Перем fileName Экспорт;

// extension - Строка - Формат документа
Перем extension Экспорт;

// document - Строка - Документ в кодировке base64
Перем document Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("fileName") <> Неопределено Тогда
		fileName = Значения.Получить("fileName");
	КонецЕсли;
	Если Значения.Получить("extension") <> Неопределено Тогда
		extension = Значения.Получить("extension");
	КонецЕсли;
	Если Значения.Получить("document") <> Неопределено Тогда
		document = Значения.Получить("document");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если fileName <> Неопределено Тогда
		Результат.Вставить("fileName", ЗначениеДляJSON(fileName));
	КонецЕсли;
	Если extension <> Неопределено Тогда
		Результат.Вставить("extension", ЗначениеДляJSON(extension));
	КонецЕсли;
	Если document <> Неопределено Тогда
		Результат.Вставить("document", ЗначениеДляJSON(document));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

