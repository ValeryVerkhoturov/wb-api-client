// FinancesSalesReportListReq
//
// Параметры запроса
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// dateFrom - Строка - Начальная дата отчёта. Можно передать дату или дату со временем. Время можно указывать с точностью до секунд или миллисекунд. Дата передаётся в формате [RFC3339…
Перем dateFrom Экспорт;

// dateTo - Строка - Конечная дата отчёта. Дата в формате [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339). Можно передать дату или дату со временем. Время можно указывать с…
Перем dateTo Экспорт;

// limit - Число - Количество отчётов в ответе
Перем limit Экспорт;

// offset - Число - Сколько элементов пропустить. Например, для значения `10` ответ начнётся с 11 элемента
Перем offset Экспорт;

// period - Строка - Периодичность отчётов: - `weekly` — еженедельные - `daily` — ежедневные
Перем period Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("dateFrom") <> Неопределено Тогда
		dateFrom = Значения.Получить("dateFrom");
	КонецЕсли;
	Если Значения.Получить("dateTo") <> Неопределено Тогда
		dateTo = Значения.Получить("dateTo");
	КонецЕсли;
	Если Значения.Получить("limit") <> Неопределено Тогда
		limit = Значения.Получить("limit");
	КонецЕсли;
	Если Значения.Получить("offset") <> Неопределено Тогда
		offset = Значения.Получить("offset");
	КонецЕсли;
	Если Значения.Получить("period") <> Неопределено Тогда
		period = Значения.Получить("period");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если dateFrom <> Неопределено Тогда
		Результат.Вставить("dateFrom", ЗначениеДляJSON(dateFrom));
	КонецЕсли;
	Если dateTo <> Неопределено Тогда
		Результат.Вставить("dateTo", ЗначениеДляJSON(dateTo));
	КонецЕсли;
	Если limit <> Неопределено Тогда
		Результат.Вставить("limit", ЗначениеДляJSON(limit));
	КонецЕсли;
	Если offset <> Неопределено Тогда
		Результат.Вставить("offset", ЗначениеДляJSON(offset));
	КонецЕсли;
	Если period <> Неопределено Тогда
		Результат.Вставить("period", ЗначениеДляJSON(period));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

