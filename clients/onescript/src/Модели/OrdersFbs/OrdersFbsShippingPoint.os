// OrdersFbsShippingPoint
//
// Данные пункта отгрузки
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// id - Число - ID пункта отгрузки
Перем id Экспорт;

// name - Строка - Название
Перем name Экспорт;

// address - Строка - Адрес
Перем address Экспорт;

// city - Строка - Населённый пункт
Перем city Экспорт;

// officeType - Строка - Тип пункта отгрузки: - `sc` — сортировочный центр - `sw` — склад - `pp` — ПВЗ
Перем officeType Экспорт;

// cargoTypes - Массив - Типы товаров, которые принимает пункт отгрузки: - `1` — малогабаритный товар (МГТ) - `2` — сверхгабаритный товар (СГТ) - `3` — крупногабаритный товар (КГТ+)
Перем cargoTypes Экспорт;

// latitude - Число - Широта
Перем latitude Экспорт;

// longitude - Число - Долгота
Перем longitude Экспорт;

// fulfillment - Булево - Услуга **Фулфилмент в СЦ** для поставки по модели FBS: - `true` — доступна - `false` — недоступна
Перем fulfillment Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("id") <> Неопределено Тогда
		id = Значения.Получить("id");
	КонецЕсли;
	Если Значения.Получить("name") <> Неопределено Тогда
		name = Значения.Получить("name");
	КонецЕсли;
	Если Значения.Получить("address") <> Неопределено Тогда
		address = Значения.Получить("address");
	КонецЕсли;
	Если Значения.Получить("city") <> Неопределено Тогда
		city = Значения.Получить("city");
	КонецЕсли;
	Если Значения.Получить("officeType") <> Неопределено Тогда
		officeType = Значения.Получить("officeType");
	КонецЕсли;
	Если Значения.Получить("cargoTypes") <> Неопределено Тогда
		cargoTypes = Значения.Получить("cargoTypes");
	КонецЕсли;
	Если Значения.Получить("latitude") <> Неопределено Тогда
		latitude = Значения.Получить("latitude");
	КонецЕсли;
	Если Значения.Получить("longitude") <> Неопределено Тогда
		longitude = Значения.Получить("longitude");
	КонецЕсли;
	Если Значения.Получить("fulfillment") <> Неопределено Тогда
		fulfillment = Значения.Получить("fulfillment");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если id <> Неопределено Тогда
		Результат.Вставить("id", ЗначениеДляJSON(id));
	КонецЕсли;
	Если name <> Неопределено Тогда
		Результат.Вставить("name", ЗначениеДляJSON(name));
	КонецЕсли;
	Если address <> Неопределено Тогда
		Результат.Вставить("address", ЗначениеДляJSON(address));
	КонецЕсли;
	Если city <> Неопределено Тогда
		Результат.Вставить("city", ЗначениеДляJSON(city));
	КонецЕсли;
	Если officeType <> Неопределено Тогда
		Результат.Вставить("officeType", ЗначениеДляJSON(officeType));
	КонецЕсли;
	Если cargoTypes <> Неопределено Тогда
		Результат.Вставить("cargoTypes", ЗначениеДляJSON(cargoTypes));
	КонецЕсли;
	Если latitude <> Неопределено Тогда
		Результат.Вставить("latitude", ЗначениеДляJSON(latitude));
	КонецЕсли;
	Если longitude <> Неопределено Тогда
		Результат.Вставить("longitude", ЗначениеДляJSON(longitude));
	КонецЕсли;
	Если fulfillment <> Неопределено Тогда
		Результат.Вставить("fulfillment", ЗначениеДляJSON(fulfillment));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

