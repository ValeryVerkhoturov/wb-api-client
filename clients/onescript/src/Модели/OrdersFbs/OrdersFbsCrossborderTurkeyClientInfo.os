// OrdersFbsCrossborderTurkeyClientInfo
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// firstName - Строка - Имя клиента
Перем firstName Экспорт;

// fullName - Строка - Фамилия, Имя, Отчество
Перем fullName Экспорт;

// lastName - Строка - Фамилия клиента
Перем lastName Экспорт;

// middleName - Строка - Отчество клиента
Перем middleName Экспорт;

// orderID - Число - Номер заказа
Перем orderID Экспорт;

// phone - Строка - Телефон для связи с клиентом
Перем phone Экспорт;

// phoneCode - Строка - Не используется
Перем phoneCode Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("firstName") <> Неопределено Тогда
		firstName = Значения.Получить("firstName");
	КонецЕсли;
	Если Значения.Получить("fullName") <> Неопределено Тогда
		fullName = Значения.Получить("fullName");
	КонецЕсли;
	Если Значения.Получить("lastName") <> Неопределено Тогда
		lastName = Значения.Получить("lastName");
	КонецЕсли;
	Если Значения.Получить("middleName") <> Неопределено Тогда
		middleName = Значения.Получить("middleName");
	КонецЕсли;
	Если Значения.Получить("orderID") <> Неопределено Тогда
		orderID = Значения.Получить("orderID");
	КонецЕсли;
	Если Значения.Получить("phone") <> Неопределено Тогда
		phone = Значения.Получить("phone");
	КонецЕсли;
	Если Значения.Получить("phoneCode") <> Неопределено Тогда
		phoneCode = Значения.Получить("phoneCode");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если firstName <> Неопределено Тогда
		Результат.Вставить("firstName", ЗначениеДляJSON(firstName));
	КонецЕсли;
	Если fullName <> Неопределено Тогда
		Результат.Вставить("fullName", ЗначениеДляJSON(fullName));
	КонецЕсли;
	Если lastName <> Неопределено Тогда
		Результат.Вставить("lastName", ЗначениеДляJSON(lastName));
	КонецЕсли;
	Если middleName <> Неопределено Тогда
		Результат.Вставить("middleName", ЗначениеДляJSON(middleName));
	КонецЕсли;
	Если orderID <> Неопределено Тогда
		Результат.Вставить("orderID", ЗначениеДляJSON(orderID));
	КонецЕсли;
	Если phone <> Неопределено Тогда
		Результат.Вставить("phone", ЗначениеДляJSON(phone));
	КонецЕсли;
	Если phoneCode <> Неопределено Тогда
		Результат.Вставить("phoneCode", ЗначениеДляJSON(phoneCode));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

