// OrdersFbwModelsOptionsResultModelResultInnerWarehousesInner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// warehouseID - Число - ID склада. По нему можно получить [информацию о складе](./orders-fbw#tag/informationForFormingSupplies/operation/getV1Warehouses)
Перем warehouseID Экспорт;

// canBox - Булево - Тип упаковки **Короб**: - `true` — доступен - `false` — недоступен
Перем canBox Экспорт;

// canMonopallet - Булево - Тип упаковки **Монопаллета**: - `true` — доступен - `false` — недоступен
Перем canMonopallet Экспорт;

// canSupersafe - Булево - Тип упаковки **Суперсейф**: - `true` — доступен - `false` — недоступен
Перем canSupersafe Экспорт;

// isBoxOnPallet - Булево - Тип поставки **Поштучная палета**: - `true` — доступен - `false` — недоступен
Перем isBoxOnPallet Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("warehouseID") <> Неопределено Тогда
		warehouseID = Значения.Получить("warehouseID");
	КонецЕсли;
	Если Значения.Получить("canBox") <> Неопределено Тогда
		canBox = Значения.Получить("canBox");
	КонецЕсли;
	Если Значения.Получить("canMonopallet") <> Неопределено Тогда
		canMonopallet = Значения.Получить("canMonopallet");
	КонецЕсли;
	Если Значения.Получить("canSupersafe") <> Неопределено Тогда
		canSupersafe = Значения.Получить("canSupersafe");
	КонецЕсли;
	Если Значения.Получить("isBoxOnPallet") <> Неопределено Тогда
		isBoxOnPallet = Значения.Получить("isBoxOnPallet");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если warehouseID <> Неопределено Тогда
		Результат.Вставить("warehouseID", ЗначениеДляJSON(warehouseID));
	КонецЕсли;
	Если canBox <> Неопределено Тогда
		Результат.Вставить("canBox", ЗначениеДляJSON(canBox));
	КонецЕсли;
	Если canMonopallet <> Неопределено Тогда
		Результат.Вставить("canMonopallet", ЗначениеДляJSON(canMonopallet));
	КонецЕсли;
	Если canSupersafe <> Неопределено Тогда
		Результат.Вставить("canSupersafe", ЗначениеДляJSON(canSupersafe));
	КонецЕсли;
	Если isBoxOnPallet <> Неопределено Тогда
		Результат.Вставить("isBoxOnPallet", ЗначениеДляJSON(isBoxOnPallet));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

