// DbsPostV3DbsGroupsInfoResponse200Inner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// groupID - Строка - ID группы сборочных заданий
Перем groupID Экспорт;

// deliveryCost - Число - Стоимость платной доставки в валюте продажи, умноженная на 100
Перем deliveryCost Экспорт;

// convertedDeliveryCost - Число - Стоимость платной доставки в валюте страны продавца, умноженная на 100. Предоставляется в информационных целях.
Перем convertedDeliveryCost Экспорт;

// currencyCode - Число - Код валюты продажи
Перем currencyCode Экспорт;

// convertedCurrencyCode - Число - Код валюты страны продавца
Перем convertedCurrencyCode Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("groupID") <> Неопределено Тогда
		groupID = Значения.Получить("groupID");
	КонецЕсли;
	Если Значения.Получить("deliveryCost") <> Неопределено Тогда
		deliveryCost = Значения.Получить("deliveryCost");
	КонецЕсли;
	Если Значения.Получить("convertedDeliveryCost") <> Неопределено Тогда
		convertedDeliveryCost = Значения.Получить("convertedDeliveryCost");
	КонецЕсли;
	Если Значения.Получить("currencyCode") <> Неопределено Тогда
		currencyCode = Значения.Получить("currencyCode");
	КонецЕсли;
	Если Значения.Получить("convertedCurrencyCode") <> Неопределено Тогда
		convertedCurrencyCode = Значения.Получить("convertedCurrencyCode");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если groupID <> Неопределено Тогда
		Результат.Вставить("groupID", ЗначениеДляJSON(groupID));
	КонецЕсли;
	Если deliveryCost <> Неопределено Тогда
		Результат.Вставить("deliveryCost", ЗначениеДляJSON(deliveryCost));
	КонецЕсли;
	Если convertedDeliveryCost <> Неопределено Тогда
		Результат.Вставить("convertedDeliveryCost", ЗначениеДляJSON(convertedDeliveryCost));
	КонецЕсли;
	Если currencyCode <> Неопределено Тогда
		Результат.Вставить("currencyCode", ЗначениеДляJSON(currencyCode));
	КонецЕсли;
	Если convertedCurrencyCode <> Неопределено Тогда
		Результат.Вставить("convertedCurrencyCode", ЗначениеДляJSON(convertedCurrencyCode));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

