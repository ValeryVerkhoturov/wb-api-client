// AnalyticsTableShippingOfficeItem
//
// Данные по региону отгрузки
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// regionName - Строка - Регион отгрузки. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) для складов WB может быть только `Склад WB`
Перем regionName Экспорт;

// metrics - TableShippingOfficeMetrics - Метрики по региону
Перем metrics Экспорт;

// offices - Массив - Данные по складам. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) может быть только `[]`
Перем offices Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("regionName") <> Неопределено Тогда
		regionName = Значения.Получить("regionName");
	КонецЕсли;
	Если Значения.Получить("metrics") <> Неопределено Тогда
		metrics = Значения.Получить("metrics");
	КонецЕсли;
	Если Значения.Получить("offices") <> Неопределено Тогда
		offices = Значения.Получить("offices");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если regionName <> Неопределено Тогда
		Результат.Вставить("regionName", ЗначениеДляJSON(regionName));
	КонецЕсли;
	Если metrics <> Неопределено Тогда
		Результат.Вставить("metrics", ЗначениеДляJSON(metrics));
	КонецЕсли;
	Если offices <> Неопределено Тогда
		Результат.Вставить("offices", ЗначениеДляJSON(offices));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

