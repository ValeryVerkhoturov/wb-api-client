// ItemsPostV2GetCardsListResponse200CardsInnerPhotosInner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// big - Строка - URL фото `900x1200`
Перем big Экспорт;

// c246x328 - Строка - URL фото `248x328`
Перем c246x328 Экспорт;

// c516x688 - Строка - URL фото `516x688`
Перем c516x688 Экспорт;

// square - Строка - URL фото `600x600`
Перем square Экспорт;

// tm - Строка - URL фото `75x100`
Перем tm Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("big") <> Неопределено Тогда
		big = Значения.Получить("big");
	КонецЕсли;
	Если Значения.Получить("c246x328") <> Неопределено Тогда
		c246x328 = Значения.Получить("c246x328");
	КонецЕсли;
	Если Значения.Получить("c516x688") <> Неопределено Тогда
		c516x688 = Значения.Получить("c516x688");
	КонецЕсли;
	Если Значения.Получить("square") <> Неопределено Тогда
		square = Значения.Получить("square");
	КонецЕсли;
	Если Значения.Получить("tm") <> Неопределено Тогда
		tm = Значения.Получить("tm");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если big <> Неопределено Тогда
		Результат.Вставить("big", ЗначениеДляJSON(big));
	КонецЕсли;
	Если c246x328 <> Неопределено Тогда
		Результат.Вставить("c246x328", ЗначениеДляJSON(c246x328));
	КонецЕсли;
	Если c516x688 <> Неопределено Тогда
		Результат.Вставить("c516x688", ЗначениеДляJSON(c516x688));
	КонецЕсли;
	Если square <> Неопределено Тогда
		Результат.Вставить("square", ЗначениеДляJSON(square));
	КонецЕсли;
	Если tm <> Неопределено Тогда
		Результат.Вставить("tm", ЗначениеДляJSON(tm));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

