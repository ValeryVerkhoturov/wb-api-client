// ItemsPostV2GetCardsListResponse200CardsInnerDocumentsItemsInnerVerdict
//
// Результат проверки документа. Возвращается, когда проверка завершена
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// verified - Булево - - `true` — документ проверен - `false` — документ не проверен
Перем verified Экспорт;

// status - Число - Результат проверки документа: - `1` — проверка пройдена - `2` — проверка не пройдена
Перем status Экспорт;

// reason - Строка - Ошибка при проверке, возвращается для `status: 2`. Возможные значения: - `document\\_missing` — Документ не загружен - `document\\_not\\_found` — Документ не на…
Перем reason Экспорт;

// additionalData - Соответствие - Дополнительная информация
Перем additionalData Экспорт;

// createdAt - Дата - Дата проверки документа
Перем createdAt Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("verified") <> Неопределено Тогда
		verified = Значения.Получить("verified");
	КонецЕсли;
	Если Значения.Получить("status") <> Неопределено Тогда
		status = Значения.Получить("status");
	КонецЕсли;
	Если Значения.Получить("reason") <> Неопределено Тогда
		reason = Значения.Получить("reason");
	КонецЕсли;
	Если Значения.Получить("additionalData") <> Неопределено Тогда
		additionalData = Значения.Получить("additionalData");
	КонецЕсли;
	Если Значения.Получить("createdAt") <> Неопределено Тогда
		createdAt = Значения.Получить("createdAt");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если verified <> Неопределено Тогда
		Результат.Вставить("verified", ЗначениеДляJSON(verified));
	КонецЕсли;
	Если status <> Неопределено Тогда
		Результат.Вставить("status", ЗначениеДляJSON(status));
	КонецЕсли;
	Если reason <> Неопределено Тогда
		Результат.Вставить("reason", ЗначениеДляJSON(reason));
	КонецЕсли;
	Если additionalData <> Неопределено Тогда
		Результат.Вставить("additionalData", ЗначениеДляJSON(additionalData));
	КонецЕсли;
	Если createdAt <> Неопределено Тогда
		Результат.Вставить("createdAt", ЗначениеДляJSON(createdAt));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

