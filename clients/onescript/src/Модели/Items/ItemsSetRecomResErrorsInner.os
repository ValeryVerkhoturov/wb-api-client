// ItemsSetRecomResErrorsInner
//
// Ошибки установки рекомендаций
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// mainNm - Строка - Значение параметра `nmId`
Перем mainNm Экспорт;

// recomNm - Строка - Значение параметра `recomNm`
Перем recomNm Экспорт;

// message - Строка - Сообщение об ошибке
Перем message Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("mainNm") <> Неопределено Тогда
		mainNm = Значения.Получить("mainNm");
	КонецЕсли;
	Если Значения.Получить("recomNm") <> Неопределено Тогда
		recomNm = Значения.Получить("recomNm");
	КонецЕсли;
	Если Значения.Получить("message") <> Неопределено Тогда
		message = Значения.Получить("message");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если mainNm <> Неопределено Тогда
		Результат.Вставить("mainNm", ЗначениеДляJSON(mainNm));
	КонецЕсли;
	Если recomNm <> Неопределено Тогда
		Результат.Вставить("recomNm", ЗначениеДляJSON(recomNm));
	КонецЕсли;
	Если message <> Неопределено Тогда
		Результат.Вставить("message", ЗначениеДляJSON(message));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

