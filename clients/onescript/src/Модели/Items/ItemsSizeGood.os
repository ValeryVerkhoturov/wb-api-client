// ItemsSizeGood
//
// Информация о размере
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// nmID - Число - Артикул WB
Перем nmID Экспорт;

// sizeID - Число - ID размера. Можно получить с помощью метода [Получение списка товаров по артикулам](./item-management#tag/pricesAndDiscounts/operation/getV2ListGoodsFilter), по…
Перем sizeID Экспорт;

// vendorCode - Строка - Артикул продавца
Перем vendorCode Экспорт;

// price - Число - Цена
Перем price Экспорт;

// currencyIsoCode4217 - Строка - Валюта, по стандарту ISO 4217
Перем currencyIsoCode4217 Экспорт;

// discountedPrice - Число - Цена со скидкой
Перем discountedPrice Экспорт;

// clubDiscountedPrice - Число - Цена со скидкой, включая скидку WB Клуба
Перем clubDiscountedPrice Экспорт;

// discount - Число - Скидка, %
Перем discount Экспорт;

// clubDiscount - Число - Скидка WB Клуба, %
Перем clubDiscount Экспорт;

// techSizeName - Строка - Размер товара
Перем techSizeName Экспорт;

// editableSizePrice - Булево - Можно ли устанавливать цены отдельно для разных размеров (зависит от категории товара): - `true` — можно - `false` — нельзя
Перем editableSizePrice Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("nmID") <> Неопределено Тогда
		nmID = Значения.Получить("nmID");
	КонецЕсли;
	Если Значения.Получить("sizeID") <> Неопределено Тогда
		sizeID = Значения.Получить("sizeID");
	КонецЕсли;
	Если Значения.Получить("vendorCode") <> Неопределено Тогда
		vendorCode = Значения.Получить("vendorCode");
	КонецЕсли;
	Если Значения.Получить("price") <> Неопределено Тогда
		price = Значения.Получить("price");
	КонецЕсли;
	Если Значения.Получить("currencyIsoCode4217") <> Неопределено Тогда
		currencyIsoCode4217 = Значения.Получить("currencyIsoCode4217");
	КонецЕсли;
	Если Значения.Получить("discountedPrice") <> Неопределено Тогда
		discountedPrice = Значения.Получить("discountedPrice");
	КонецЕсли;
	Если Значения.Получить("clubDiscountedPrice") <> Неопределено Тогда
		clubDiscountedPrice = Значения.Получить("clubDiscountedPrice");
	КонецЕсли;
	Если Значения.Получить("discount") <> Неопределено Тогда
		discount = Значения.Получить("discount");
	КонецЕсли;
	Если Значения.Получить("clubDiscount") <> Неопределено Тогда
		clubDiscount = Значения.Получить("clubDiscount");
	КонецЕсли;
	Если Значения.Получить("techSizeName") <> Неопределено Тогда
		techSizeName = Значения.Получить("techSizeName");
	КонецЕсли;
	Если Значения.Получить("editableSizePrice") <> Неопределено Тогда
		editableSizePrice = Значения.Получить("editableSizePrice");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если nmID <> Неопределено Тогда
		Результат.Вставить("nmID", ЗначениеДляJSON(nmID));
	КонецЕсли;
	Если sizeID <> Неопределено Тогда
		Результат.Вставить("sizeID", ЗначениеДляJSON(sizeID));
	КонецЕсли;
	Если vendorCode <> Неопределено Тогда
		Результат.Вставить("vendorCode", ЗначениеДляJSON(vendorCode));
	КонецЕсли;
	Если price <> Неопределено Тогда
		Результат.Вставить("price", ЗначениеДляJSON(price));
	КонецЕсли;
	Если currencyIsoCode4217 <> Неопределено Тогда
		Результат.Вставить("currencyIsoCode4217", ЗначениеДляJSON(currencyIsoCode4217));
	КонецЕсли;
	Если discountedPrice <> Неопределено Тогда
		Результат.Вставить("discountedPrice", ЗначениеДляJSON(discountedPrice));
	КонецЕсли;
	Если clubDiscountedPrice <> Неопределено Тогда
		Результат.Вставить("clubDiscountedPrice", ЗначениеДляJSON(clubDiscountedPrice));
	КонецЕсли;
	Если discount <> Неопределено Тогда
		Результат.Вставить("discount", ЗначениеДляJSON(discount));
	КонецЕсли;
	Если clubDiscount <> Неопределено Тогда
		Результат.Вставить("clubDiscount", ЗначениеДляJSON(clubDiscount));
	КонецЕсли;
	Если techSizeName <> Неопределено Тогда
		Результат.Вставить("techSizeName", ЗначениеДляJSON(techSizeName));
	КонецЕсли;
	Если editableSizePrice <> Неопределено Тогда
		Результат.Вставить("editableSizePrice", ЗначениеДляJSON(editableSizePrice));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

