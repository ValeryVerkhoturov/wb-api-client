// CommunicationsOpenapiPinReviewItemResultDataInner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// feedbackId - Строка - ID отзыва
Перем feedbackId Экспорт;

// pinId - Число - ID операции закрепления. Если поле отсутствует — закрепить отзыв не удалось
Перем pinId Экспорт;

// pinMethod - Строка - Метод закрепления: - `subscription` — подписка Джем - `tariff` — тарифная опция
Перем pinMethod Экспорт;

// pinOn - Строка - Место закрепления отзыва: - `nm` — карточка товара - `imt` — группа [объединённых](https://dev.wildberries.ru/knowledge-base/articles/019d49a4-1320-71bb-9dac-8b…
Перем pinOn Экспорт;

// isErrors - Булево - Есть ли ошибки
Перем isErrors Экспорт;

// errors - Массив - Детали ошибок
Перем errors Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("feedbackId") <> Неопределено Тогда
		feedbackId = Значения.Получить("feedbackId");
	КонецЕсли;
	Если Значения.Получить("pinId") <> Неопределено Тогда
		pinId = Значения.Получить("pinId");
	КонецЕсли;
	Если Значения.Получить("pinMethod") <> Неопределено Тогда
		pinMethod = Значения.Получить("pinMethod");
	КонецЕсли;
	Если Значения.Получить("pinOn") <> Неопределено Тогда
		pinOn = Значения.Получить("pinOn");
	КонецЕсли;
	Если Значения.Получить("isErrors") <> Неопределено Тогда
		isErrors = Значения.Получить("isErrors");
	КонецЕсли;
	Если Значения.Получить("errors") <> Неопределено Тогда
		errors = Значения.Получить("errors");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если feedbackId <> Неопределено Тогда
		Результат.Вставить("feedbackId", ЗначениеДляJSON(feedbackId));
	КонецЕсли;
	Если pinId <> Неопределено Тогда
		Результат.Вставить("pinId", ЗначениеДляJSON(pinId));
	КонецЕсли;
	Если pinMethod <> Неопределено Тогда
		Результат.Вставить("pinMethod", ЗначениеДляJSON(pinMethod));
	КонецЕсли;
	Если pinOn <> Неопределено Тогда
		Результат.Вставить("pinOn", ЗначениеДляJSON(pinOn));
	КонецЕсли;
	Если isErrors <> Неопределено Тогда
		Результат.Вставить("isErrors", ЗначениеДляJSON(isErrors));
	КонецЕсли;
	Если errors <> Неопределено Тогда
		Результат.Вставить("errors", ЗначениеДляJSON(errors));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

