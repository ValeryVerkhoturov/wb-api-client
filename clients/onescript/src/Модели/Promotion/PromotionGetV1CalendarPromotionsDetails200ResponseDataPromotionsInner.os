// PromotionGetV1CalendarPromotionsDetails200ResponseDataPromotionsInner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// id - Число - ID акции
Перем id Экспорт;

// name - Строка - Название акции
Перем name Экспорт;

// description - Строка - Описание акции
Перем description Экспорт;

// advantages - Массив - Преимущества акции
Перем advantages Экспорт;

// startDateTime - Строка - Начало акции
Перем startDateTime Экспорт;

// endDateTime - Строка - Конец акции
Перем endDateTime Экспорт;

// inPromoActionLeftovers - Число - Количество товаров с остатками, участвующих в акции
Перем inPromoActionLeftovers Экспорт;

// inPromoActionTotal - Число - Общее количество товаров, участвующих в акции
Перем inPromoActionTotal Экспорт;

// notInPromoActionLeftovers - Число - Количество товаров с остатками, не участвующих в акции
Перем notInPromoActionLeftovers Экспорт;

// notInPromoActionTotal - Число - Общее количество товаров, не участвующих в акции
Перем notInPromoActionTotal Экспорт;

// participationPercentage - Число - Уже участвующие в акции товары, %. Рассчитывается по товарам в акции и с остатком
Перем participationPercentage Экспорт;

// type - Строка - Тип акции: - `regular` — акция - `auto` — автоакция
Перем type Экспорт;

// exceptionProductsCount - Число - Количество товаров, исключенных из автоакции до её старта. Только при `\"type\": \"auto\"`. В момент старта акции эти товары автоматически будут без скидки
Перем exceptionProductsCount Экспорт;

// ranging - Массив - Ранжирование (если подключено)
Перем ranging Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("id") <> Неопределено Тогда
		id = Значения.Получить("id");
	КонецЕсли;
	Если Значения.Получить("name") <> Неопределено Тогда
		name = Значения.Получить("name");
	КонецЕсли;
	Если Значения.Получить("description") <> Неопределено Тогда
		description = Значения.Получить("description");
	КонецЕсли;
	Если Значения.Получить("advantages") <> Неопределено Тогда
		advantages = Значения.Получить("advantages");
	КонецЕсли;
	Если Значения.Получить("startDateTime") <> Неопределено Тогда
		startDateTime = Значения.Получить("startDateTime");
	КонецЕсли;
	Если Значения.Получить("endDateTime") <> Неопределено Тогда
		endDateTime = Значения.Получить("endDateTime");
	КонецЕсли;
	Если Значения.Получить("inPromoActionLeftovers") <> Неопределено Тогда
		inPromoActionLeftovers = Значения.Получить("inPromoActionLeftovers");
	КонецЕсли;
	Если Значения.Получить("inPromoActionTotal") <> Неопределено Тогда
		inPromoActionTotal = Значения.Получить("inPromoActionTotal");
	КонецЕсли;
	Если Значения.Получить("notInPromoActionLeftovers") <> Неопределено Тогда
		notInPromoActionLeftovers = Значения.Получить("notInPromoActionLeftovers");
	КонецЕсли;
	Если Значения.Получить("notInPromoActionTotal") <> Неопределено Тогда
		notInPromoActionTotal = Значения.Получить("notInPromoActionTotal");
	КонецЕсли;
	Если Значения.Получить("participationPercentage") <> Неопределено Тогда
		participationPercentage = Значения.Получить("participationPercentage");
	КонецЕсли;
	Если Значения.Получить("type") <> Неопределено Тогда
		type = Значения.Получить("type");
	КонецЕсли;
	Если Значения.Получить("exceptionProductsCount") <> Неопределено Тогда
		exceptionProductsCount = Значения.Получить("exceptionProductsCount");
	КонецЕсли;
	Если Значения.Получить("ranging") <> Неопределено Тогда
		ranging = Значения.Получить("ranging");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если id <> Неопределено Тогда
		Результат.Вставить("id", ЗначениеДляJSON(id));
	КонецЕсли;
	Если name <> Неопределено Тогда
		Результат.Вставить("name", ЗначениеДляJSON(name));
	КонецЕсли;
	Если description <> Неопределено Тогда
		Результат.Вставить("description", ЗначениеДляJSON(description));
	КонецЕсли;
	Если advantages <> Неопределено Тогда
		Результат.Вставить("advantages", ЗначениеДляJSON(advantages));
	КонецЕсли;
	Если startDateTime <> Неопределено Тогда
		Результат.Вставить("startDateTime", ЗначениеДляJSON(startDateTime));
	КонецЕсли;
	Если endDateTime <> Неопределено Тогда
		Результат.Вставить("endDateTime", ЗначениеДляJSON(endDateTime));
	КонецЕсли;
	Если inPromoActionLeftovers <> Неопределено Тогда
		Результат.Вставить("inPromoActionLeftovers", ЗначениеДляJSON(inPromoActionLeftovers));
	КонецЕсли;
	Если inPromoActionTotal <> Неопределено Тогда
		Результат.Вставить("inPromoActionTotal", ЗначениеДляJSON(inPromoActionTotal));
	КонецЕсли;
	Если notInPromoActionLeftovers <> Неопределено Тогда
		Результат.Вставить("notInPromoActionLeftovers", ЗначениеДляJSON(notInPromoActionLeftovers));
	КонецЕсли;
	Если notInPromoActionTotal <> Неопределено Тогда
		Результат.Вставить("notInPromoActionTotal", ЗначениеДляJSON(notInPromoActionTotal));
	КонецЕсли;
	Если participationPercentage <> Неопределено Тогда
		Результат.Вставить("participationPercentage", ЗначениеДляJSON(participationPercentage));
	КонецЕсли;
	Если type <> Неопределено Тогда
		Результат.Вставить("type", ЗначениеДляJSON(type));
	КонецЕсли;
	Если exceptionProductsCount <> Неопределено Тогда
		Результат.Вставить("exceptionProductsCount", ЗначениеДляJSON(exceptionProductsCount));
	КонецЕсли;
	Если ranging <> Неопределено Тогда
		Результат.Вставить("ranging", ЗначениеДляJSON(ranging));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

