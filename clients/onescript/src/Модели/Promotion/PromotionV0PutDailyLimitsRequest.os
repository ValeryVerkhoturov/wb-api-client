// PromotionV0PutDailyLimitsRequest
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// advertIds - Массив - ID кампаний
Перем advertIds Экспорт;

// enabled - Булево - Включить лимит: - `true` — да - `false` — нет
Перем enabled Экспорт;

// dailyLimit - Число - Сумма дневного лимита. Параметр обязателен при `\"enabled\": true`. Минимально допустимая сумма указана в поле `minDailyLimit` метода [GET /api/advert/v1/config…
Перем dailyLimit Экспорт;

// carryOverEnabled - Булево - Переносить неиспользованный остаток лимита на следующий день: - `true` — да - `false` — нет Параметр обязателен при `\"enabled\": true`
Перем carryOverEnabled Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("advertIds") <> Неопределено Тогда
		advertIds = Значения.Получить("advertIds");
	КонецЕсли;
	Если Значения.Получить("enabled") <> Неопределено Тогда
		enabled = Значения.Получить("enabled");
	КонецЕсли;
	Если Значения.Получить("dailyLimit") <> Неопределено Тогда
		dailyLimit = Значения.Получить("dailyLimit");
	КонецЕсли;
	Если Значения.Получить("carryOverEnabled") <> Неопределено Тогда
		carryOverEnabled = Значения.Получить("carryOverEnabled");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если advertIds <> Неопределено Тогда
		Результат.Вставить("advertIds", ЗначениеДляJSON(advertIds));
	КонецЕсли;
	Если enabled <> Неопределено Тогда
		Результат.Вставить("enabled", ЗначениеДляJSON(enabled));
	КонецЕсли;
	Если dailyLimit <> Неопределено Тогда
		Результат.Вставить("dailyLimit", ЗначениеДляJSON(dailyLimit));
	КонецЕсли;
	Если carryOverEnabled <> Неопределено Тогда
		Результат.Вставить("carryOverEnabled", ЗначениеДляJSON(carryOverEnabled));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

