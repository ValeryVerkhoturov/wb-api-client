// ReportsGetV1PaidStorageTasksTaskIdDownloadResponse200Inner
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// date - Строка - Дата, за которую был расчёт или перерасчёт
Перем date Экспорт;

// logWarehouseCoef - Число - Коэффициент логистики и хранения. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) может быть только `0`
Перем logWarehouseCoef Экспорт;

// officeId - Число - ID склада. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) может быть только `0`
Перем officeId Экспорт;

// warehouse - Строка - Название склада. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) может быть только `Склад WB РФ`
Перем warehouse Экспорт;

// warehouseCoef - Число - Коэффициент хранения
Перем warehouseCoef Экспорт;

// giId - Число - ID поставки
Перем giId Экспорт;

// chrtId - Число - ID размера для этого артикула WB
Перем chrtId Экспорт;

// size - Строка - Размер (`techSize` в карточке товара)
Перем size Экспорт;

// barcode - Строка - Баркод
Перем barcode Экспорт;

// subject - Строка - Предмет
Перем subject Экспорт;

// brand - Строка - Бренд
Перем brand Экспорт;

// vendorCode - Строка - Артикул продавца
Перем vendorCode Экспорт;

// nmId - Число - Артикул WB
Перем nmId Экспорт;

// volume - Число - Объём товара
Перем volume Экспорт;

// calcType - Строка - Способ расчёта
Перем calcType Экспорт;

// warehousePrice - Число - Сумма хранения
Перем warehousePrice Экспорт;

// barcodesCount - Число - Количество единиц товара (штук), подлежащих тарифицированию за расчётные сутки
Перем barcodesCount Экспорт;

// palletPlaceCode - Число - Код паллетоместа. [На данный момент](https://dev.wildberries.ru/release-notes?id=570) может быть только `0`
Перем palletPlaceCode Экспорт;

// palletCount - Число - Количество паллет
Перем palletCount Экспорт;

// originalDate - Строка - Если был перерасчёт, это дата первоначального расчёта. Если перерасчёта не было, совпадает с `date`
Перем originalDate Экспорт;

// loyaltyDiscount - Число - Скидка программы лояльности, ₽
Перем loyaltyDiscount Экспорт;

// tariffFixDate - Строка - Дата фиксации тарифа
Перем tariffFixDate Экспорт;

// tariffLowerDate - Строка - Дата понижения тарифа
Перем tariffLowerDate Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("date") <> Неопределено Тогда
		date = Значения.Получить("date");
	КонецЕсли;
	Если Значения.Получить("logWarehouseCoef") <> Неопределено Тогда
		logWarehouseCoef = Значения.Получить("logWarehouseCoef");
	КонецЕсли;
	Если Значения.Получить("officeId") <> Неопределено Тогда
		officeId = Значения.Получить("officeId");
	КонецЕсли;
	Если Значения.Получить("warehouse") <> Неопределено Тогда
		warehouse = Значения.Получить("warehouse");
	КонецЕсли;
	Если Значения.Получить("warehouseCoef") <> Неопределено Тогда
		warehouseCoef = Значения.Получить("warehouseCoef");
	КонецЕсли;
	Если Значения.Получить("giId") <> Неопределено Тогда
		giId = Значения.Получить("giId");
	КонецЕсли;
	Если Значения.Получить("chrtId") <> Неопределено Тогда
		chrtId = Значения.Получить("chrtId");
	КонецЕсли;
	Если Значения.Получить("size") <> Неопределено Тогда
		size = Значения.Получить("size");
	КонецЕсли;
	Если Значения.Получить("barcode") <> Неопределено Тогда
		barcode = Значения.Получить("barcode");
	КонецЕсли;
	Если Значения.Получить("subject") <> Неопределено Тогда
		subject = Значения.Получить("subject");
	КонецЕсли;
	Если Значения.Получить("brand") <> Неопределено Тогда
		brand = Значения.Получить("brand");
	КонецЕсли;
	Если Значения.Получить("vendorCode") <> Неопределено Тогда
		vendorCode = Значения.Получить("vendorCode");
	КонецЕсли;
	Если Значения.Получить("nmId") <> Неопределено Тогда
		nmId = Значения.Получить("nmId");
	КонецЕсли;
	Если Значения.Получить("volume") <> Неопределено Тогда
		volume = Значения.Получить("volume");
	КонецЕсли;
	Если Значения.Получить("calcType") <> Неопределено Тогда
		calcType = Значения.Получить("calcType");
	КонецЕсли;
	Если Значения.Получить("warehousePrice") <> Неопределено Тогда
		warehousePrice = Значения.Получить("warehousePrice");
	КонецЕсли;
	Если Значения.Получить("barcodesCount") <> Неопределено Тогда
		barcodesCount = Значения.Получить("barcodesCount");
	КонецЕсли;
	Если Значения.Получить("palletPlaceCode") <> Неопределено Тогда
		palletPlaceCode = Значения.Получить("palletPlaceCode");
	КонецЕсли;
	Если Значения.Получить("palletCount") <> Неопределено Тогда
		palletCount = Значения.Получить("palletCount");
	КонецЕсли;
	Если Значения.Получить("originalDate") <> Неопределено Тогда
		originalDate = Значения.Получить("originalDate");
	КонецЕсли;
	Если Значения.Получить("loyaltyDiscount") <> Неопределено Тогда
		loyaltyDiscount = Значения.Получить("loyaltyDiscount");
	КонецЕсли;
	Если Значения.Получить("tariffFixDate") <> Неопределено Тогда
		tariffFixDate = Значения.Получить("tariffFixDate");
	КонецЕсли;
	Если Значения.Получить("tariffLowerDate") <> Неопределено Тогда
		tariffLowerDate = Значения.Получить("tariffLowerDate");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если date <> Неопределено Тогда
		Результат.Вставить("date", ЗначениеДляJSON(date));
	КонецЕсли;
	Если logWarehouseCoef <> Неопределено Тогда
		Результат.Вставить("logWarehouseCoef", ЗначениеДляJSON(logWarehouseCoef));
	КонецЕсли;
	Если officeId <> Неопределено Тогда
		Результат.Вставить("officeId", ЗначениеДляJSON(officeId));
	КонецЕсли;
	Если warehouse <> Неопределено Тогда
		Результат.Вставить("warehouse", ЗначениеДляJSON(warehouse));
	КонецЕсли;
	Если warehouseCoef <> Неопределено Тогда
		Результат.Вставить("warehouseCoef", ЗначениеДляJSON(warehouseCoef));
	КонецЕсли;
	Если giId <> Неопределено Тогда
		Результат.Вставить("giId", ЗначениеДляJSON(giId));
	КонецЕсли;
	Если chrtId <> Неопределено Тогда
		Результат.Вставить("chrtId", ЗначениеДляJSON(chrtId));
	КонецЕсли;
	Если size <> Неопределено Тогда
		Результат.Вставить("size", ЗначениеДляJSON(size));
	КонецЕсли;
	Если barcode <> Неопределено Тогда
		Результат.Вставить("barcode", ЗначениеДляJSON(barcode));
	КонецЕсли;
	Если subject <> Неопределено Тогда
		Результат.Вставить("subject", ЗначениеДляJSON(subject));
	КонецЕсли;
	Если brand <> Неопределено Тогда
		Результат.Вставить("brand", ЗначениеДляJSON(brand));
	КонецЕсли;
	Если vendorCode <> Неопределено Тогда
		Результат.Вставить("vendorCode", ЗначениеДляJSON(vendorCode));
	КонецЕсли;
	Если nmId <> Неопределено Тогда
		Результат.Вставить("nmId", ЗначениеДляJSON(nmId));
	КонецЕсли;
	Если volume <> Неопределено Тогда
		Результат.Вставить("volume", ЗначениеДляJSON(volume));
	КонецЕсли;
	Если calcType <> Неопределено Тогда
		Результат.Вставить("calcType", ЗначениеДляJSON(calcType));
	КонецЕсли;
	Если warehousePrice <> Неопределено Тогда
		Результат.Вставить("warehousePrice", ЗначениеДляJSON(warehousePrice));
	КонецЕсли;
	Если barcodesCount <> Неопределено Тогда
		Результат.Вставить("barcodesCount", ЗначениеДляJSON(barcodesCount));
	КонецЕсли;
	Если palletPlaceCode <> Неопределено Тогда
		Результат.Вставить("palletPlaceCode", ЗначениеДляJSON(palletPlaceCode));
	КонецЕсли;
	Если palletCount <> Неопределено Тогда
		Результат.Вставить("palletCount", ЗначениеДляJSON(palletCount));
	КонецЕсли;
	Если originalDate <> Неопределено Тогда
		Результат.Вставить("originalDate", ЗначениеДляJSON(originalDate));
	КонецЕсли;
	Если loyaltyDiscount <> Неопределено Тогда
		Результат.Вставить("loyaltyDiscount", ЗначениеДляJSON(loyaltyDiscount));
	КонецЕсли;
	Если tariffFixDate <> Неопределено Тогда
		Результат.Вставить("tariffFixDate", ЗначениеДляJSON(tariffFixDate));
	КонецЕсли;
	Если tariffLowerDate <> Неопределено Тогда
		Результат.Вставить("tariffLowerDate", ЗначениеДляJSON(tariffLowerDate));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

