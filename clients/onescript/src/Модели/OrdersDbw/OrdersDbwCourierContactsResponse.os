// OrdersDbwCourierContactsResponse
//
// Класс сформирован автоматически (onescript) из спецификации OpenAPI.
// Правки будут потеряны при следующей генерации.

// carNumber - Строка - Номер автомобиля
Перем carNumber Экспорт;

// fullName - Строка - ФИО курьера
Перем fullName Экспорт;

// phone - Строка - Номер телефона
Перем phone Экспорт;

// pTimeFrom - Дата - Дата и время, с которого прибудет курьер
Перем pTimeFrom Экспорт;

// pTimeTo - Дата - Дата и время, до которого прибудет курьер
Перем pTimeTo Экспорт;

// Параметры:
//   Данные - Соответствие, Структура - исходные значения, разобранные из JSON.
//
Процедура ПриСозданииОбъекта(Знач Данные = Неопределено)

	Если Данные <> Неопределено Тогда
		Заполнить(Данные);
	КонецЕсли;

КонецПроцедуры

// Заполняет свойства из коллекции, полученной из JSON.
//
// Параметры:
//   Данные - Соответствие, Структура - исходные значения.
//
Процедура Заполнить(Знач Данные) Экспорт

	Если Данные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Значения = Новый Соответствие;
	Для Каждого ЭлементДанных Из Данные Цикл
		Значения.Вставить(Строка(ЭлементДанных.Ключ), ЭлементДанных.Значение);
	КонецЦикла;
	Если Значения.Получить("carNumber") <> Неопределено Тогда
		carNumber = Значения.Получить("carNumber");
	КонецЕсли;
	Если Значения.Получить("fullName") <> Неопределено Тогда
		fullName = Значения.Получить("fullName");
	КонецЕсли;
	Если Значения.Получить("phone") <> Неопределено Тогда
		phone = Значения.Получить("phone");
	КонецЕсли;
	Если Значения.Получить("pTimeFrom") <> Неопределено Тогда
		pTimeFrom = Значения.Получить("pTimeFrom");
	КонецЕсли;
	Если Значения.Получить("pTimeTo") <> Неопределено Тогда
		pTimeTo = Значения.Получить("pTimeTo");
	КонецЕсли;
КонецПроцедуры

// Возвращает значения свойств под именами из спецификации.
//
// Незаполненные свойства пропускаются, поэтому в теле запроса не появится
// null там, где сервис ожидает отсутствие поля.
//
// Возвращаемое значение:
//   Соответствие
//
Функция Данные() Экспорт

	Результат = Новый Соответствие;
	Если carNumber <> Неопределено Тогда
		Результат.Вставить("carNumber", ЗначениеДляJSON(carNumber));
	КонецЕсли;
	Если fullName <> Неопределено Тогда
		Результат.Вставить("fullName", ЗначениеДляJSON(fullName));
	КонецЕсли;
	Если phone <> Неопределено Тогда
		Результат.Вставить("phone", ЗначениеДляJSON(phone));
	КонецЕсли;
	Если pTimeFrom <> Неопределено Тогда
		Результат.Вставить("pTimeFrom", ЗначениеДляJSON(pTimeFrom));
	КонецЕсли;
	Если pTimeTo <> Неопределено Тогда
		Результат.Вставить("pTimeTo", ЗначениеДляJSON(pTimeTo));
	КонецЕсли;
	Возврат Результат;

КонецФункции

Функция ЗначениеДляJSON(Знач Значение)

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Результат = Новый Массив;
		Для Каждого ЭлементМассива Из Значение Цикл
			Результат.Добавить(ЗначениеДляJSON(ЭлементМассива));
		КонецЦикла;
		Возврат Результат;
	КонецЕсли;

	// Вложенные модели раскладываются тем же способом.
	Попытка
		Возврат Значение.Данные();
	Исключение
		Возврат Значение;
	КонецПопытки;

КонецФункции

